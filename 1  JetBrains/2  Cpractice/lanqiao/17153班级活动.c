#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[])
{
    int sum=0,n,count1=0,count2=0,countN=0;
    int *num = (int *)malloc(n * sizeof(int));
    scanf("%d",&n);

    for (int i = 0; i < n; i++) {
        scanf("%d",&num[i]);
    }

    for (int i = 0; i < n; i++) {

    }

    return 0;
}
