/*雇佣兵的体力最大值为M，初始体力值为0、战斗力为N、拥有X个能量元素。
当雇佣兵的体力值恰好为M时，才可以参加一个为期M天的战斗期，战斗期结束体力值将为0。
在同一个战斗期内，雇佣兵每连续战斗n天，战斗力就会上升1点，n为当前战斗期开始时的战斗力。
一个战斗期结束后，雇佣兵需要用若干个能量元素使其体力恢复到最大值M，从而参加下一个战斗期。
每个能量元素恢复的体力值不超过当前的战斗力。每个能量元素只能使用一次。
请问：雇佣兵的战斗力最大可以到达多少。
一行包括三个整数M、N、X，相邻两个整数之间用单个空格隔开。M、N、X均为不超过10000的正整数。
输出一个整数，为雇佣兵的最大战斗力。*/

#include <stdio.h>

int main() {
    int M, N, X;
    scanf("%d %d %d", &M, &N, &X);
    
    int max_power = N; // 当前战斗力
    int stamina = M; // 初始体力值为最大体力 M

    // 在有能量元素的情况下开始战斗周期 
    while (X > 0 && stamina >= N) {
        // 消耗战斗力进行战斗
        stamina -= N;

        // 战斗力每连续N天增加1
        max_power++;

        // 为了连续战斗到达下一个战斗周期，恢复体力到最大值M
        // 每个能量元素恢复的体力值不超过当前的战斗力
        // 需要 X 个能量元素来恢复体力
        int needed_energy = (stamina < 0) ? 0 : (M - stamina + (max_power - 1)) / max_power;

        if (needed_energy > X) {
            break; // 如果需要的能量元素超过可用的，退出循环
        }
        X -= needed_energy; // 使用所需的能量元素
        stamina = M; // 体力恢复到最大值
    }

    // 输出最大的战斗力
    printf("%d\n", max_power);
    return 0;
}

}
